SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Service role key (server-side only, NEVER ship this in the Flutter app or commit it).
# Used to run migrations / admin scripts against the database.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
